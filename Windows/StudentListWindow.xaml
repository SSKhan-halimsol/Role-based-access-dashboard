<Window x:Class="MFA.Windows.StudentListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Student List" Height="600" Width="800" WindowStartupLocation="CenterScreen"
        Background="LightGray" ResizeMode="CanResize" WindowState="Maximized">

    <Grid Background="IndianRed">
        <StackPanel>
            <TextBlock Text="Student List" FontSize="20" Foreground="White" Margin="0,0,0,20"/>

            <!-- Filters -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="txtFilterRollNo" Width="100" Margin="5"/>
                <TextBox x:Name="txtFilterName" Width="150" Margin="5"/>
                <TextBox x:Name="txtFilterFatherName" Width="150" Margin="5"/>
                <DatePicker x:Name="dpFromDate" Width="120" Margin="5" SelectedDateChanged="Filter_Changed"/>
                <DatePicker x:Name="dpToDate" Width="120" Margin="5" SelectedDateChanged="Filter_Changed"/>
                <Button Content="Filter" Width="80" Margin="5" Click="BtnFilter_Click"/>
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid x:Name="dgStudents" AutoGenerateColumns="False" Height="400" IsReadOnly="True" Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Roll No" Binding="{Binding RollNo}" Width="*"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Father Name" Binding="{Binding FatherName}" Width="*"/>
                    <DataGridTextColumn Header="Enrollment Date" Binding="{Binding EnrollmentDate, StringFormat=d}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Pagination -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Previous" Width="80" Margin="5" Click="BtnPrevious_Click"/>
                <TextBlock x:Name="txtPageInfo" Foreground="White" VerticalAlignment="Center" Margin="5"/>
                <Button Content="Next" Width="80" Margin="5" Click="BtnNext_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>